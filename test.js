const fs = require('fs'); const code = fs.readFileSync('app.js', 'utf8'); const window = { open: () => {}, location: { search: '' }, localStorage: { getItem: () => null, setItem: () => {} }, addEventListener: () => {} }; const localStorage = window.localStorage; let htmlResult = ''; const document = { addEventListener: () => {}, querySelectorAll: () => [], getElementById: (id) => { if (id === 'fleetContainer') { return { set innerHTML(val) { htmlResult = val; }, get innerHTML() { return htmlResult; }, style: {} }; } return null; }, querySelector: () => null, documentElement: { setAttribute: () => {}, removeAttribute: () => {} }, body: { style: {} } }; eval(code); renderFleet(fleetData); console.log('Length:', htmlResult.length);
